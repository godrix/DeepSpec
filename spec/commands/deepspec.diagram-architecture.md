---
description: Create or update ARCHITECTURE.md for the current task with Mermaid diagrams (flow, components, sequence). Planning only — no application code unless task is already active and diagram reflects implemented state.
---

## User Input

```text
$ARGUMENTS
```

Optional: task slug, diagram focus (e.g. `sequence`, `components`, `data-flow`), or scope note.

## Outline

You are producing **`ARCHITECTURE.md`** in the **current task folder** — an ancillary doc alongside A-B-C, not a replacement.

### Step 1: Resolve task

- Use task in context (`drafts/` or `active/`). If ambiguous, ask once for slug.
- Load `APPROACH.md`, `BUSINESS_CONTEXT.md`, and `## Affected Files`.
- If no task exists, stop — run `/deepspec.create-task` first.

### Step 2: Read code (read-only)

- Open files listed in `## Affected Files` and immediate dependencies.
- Note boundaries: modules, APIs, data stores, external services.

### Step 3: Write `ARCHITECTURE.md`

Create or update `.deepspec/specs/<stage>/[slug]/ARCHITECTURE.md`.

Structure:

```markdown
# Architecture — [slug]

> Mermaid diagrams for this task. Generated by DeepSpec.

## Context

One paragraph: what this task changes and why the diagram matters.

## Diagram(s)

### <Diagram title>

\`\`\`mermaid
flowchart LR
…
\`\`\`

## Legend

- Box meanings, arrows, external systems.

## References

- Links to real files: `src/...`
```

Rules:

- At least **one** valid Mermaid block.
- Prefer `flowchart`, `sequenceDiagram`, or `C4Context` matching User Input.
- Diagram must match **approved scope** in APPROACH (no fantasy components).
- On update: merge with existing `ARCHITECTURE.md`; bump a `## Changelog` section with date.

### Step 4: Cross-link APPROACH (optional)

If APPROACH has `## Architecture` or diagram placeholder, add: _See `ARCHITECTURE.md` in this task folder._

Do **not** move Execution Plan steps into ARCHITECTURE.md.

**Next:** User reviews in the DeepSpec panel (Review) or continues planning with `/deepspec.interview`.
