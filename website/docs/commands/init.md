# Initialize

Bootstrap DeepSpec in the current project.

**Trigger:** `"Initialize DeepSpec"` or `/deepspec.init`

Creates `.deepspec/` scaffolding and generates `AGENTS.md` from a context discovery scan of the repository (stack, lint, tests, CI).

Present `AGENTS.md` for review before creating tasks.
