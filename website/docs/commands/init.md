# Initialize

Bootstrap spec.md in the current project.

**Trigger:** `"Initialize spec.md"` or `/spec.md.init`

Creates `.spec.md/` scaffolding and generates `AGENTS.md` from a context discovery scan of the repository (stack, lint, tests, CI).

Present `AGENTS.md` for review before creating tasks.
